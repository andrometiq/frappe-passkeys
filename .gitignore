# local working context — never committed
CLAUDE.local.md
local-notes/
HANDOVER.md
handover/
.claude/

# secrets
.env*
*.pem
*.key
secrets/
credentials/

# local dev benches (frappe bench trees — big, machine-specific)
dev-benches/

# build artifacts
passkeys/public/dist/
# bare (no trailing slash) so it also ignores the dev node_modules *symlinks*
# (into the bench), not just real directories, at any depth.
node_modules

# python
__pycache__/
.pytest_cache/
*.egg-info/

# OS junk
.DS_Store
Thumbs.db

# cypress run artifacts
cypress/screenshots/
cypress/videos/
