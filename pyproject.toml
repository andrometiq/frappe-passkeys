[project]
name = "passkeys"
authors = [{ name = "Frappe Passkeys Contributors" }]
description = "Passkey (WebAuthn) authentication for Frappe"
requires-python = ">=3.10"
readme = "README.md"
license = { file = "LICENSE" }
dynamic = ["version"]
dependencies = [
    "webauthn>=2.8.0,<3",
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.107.0,<18.0.0"

[tool.ruff]
line-length = 110
target-version = "py310"
# `spikes/` is dev-only WebAuthn fixture generators + selfchecks (run by the
# pre-push golden-vector check), NOT the shipped package — the CI compileall and
# import gate both scope to `passkeys/`. Keep the shipped-code ruleset (UP pyupgrade
# to py310 `dict`/`list`, etc.) off the throwaway generators. `force-exclude` makes
# the exclude apply even when pre-commit passes those paths explicitly.
extend-exclude = ["spikes"]
force-exclude = true

[tool.ruff.lint]
select = [
    "F",
    "E",
    "W",
    "I",
    "UP",
    "B",
    "RUF",
]
ignore = [
    "B017", # assertRaises(Exception) - should be more specific
    "B018", # useless expression, not assigned to anything
    "B023", # function doesn't bind loop variable - will have last iteration's value
    "B904", # raise inside except without from
    "E101", # indentation contains mixed spaces and tabs
    "E402", # module level import not at top of file
    "E501", # line too long
    "E741", # ambiguous variable name
    "F401", # "unused" imports
    "F403", # can't detect undefined names from * import
    "F405", # can't detect undefined names from * import
    "F722", # syntax error in forward type annotation
    "W191", # indentation contains tabs
    "UP030", # Use implicit references for positional format fields (translations)
    "UP031", # Use format specifiers instead of percent format
    "UP032", # Use f-string instead of `format` call (translations)
    "UP037", # quoted annotations
]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
docstring-code-format = true
